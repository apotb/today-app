/** Shown once: demo swipe tutorial on home */
export const TUTORIAL_SWIPE_KEY = 'today.tutorial.swipeDemoComplete'

/**
 * Set on first like until the user opens the Events tab; then removed.
 */
export const FIRST_LIKE_PENDING_OPEN_EVENTS_KEY = 'today.hint.firstLikePendingOpenEvents'

/**
 * Set once the user opens Events after their first like; never show that flow again.
 */
export const FIRST_LIKE_EVENTS_TAB_COMPLETE_KEY = 'today.hint.firstLikeEventsTabComplete'
